# Dependencies & build output
node_modules/
dist/

# Claude Code local tooling / skills — not part of the website
.claude/
.agents/

# Unbearbeitete Fotoquellen — die optimierten WebP-Dateien liegen in public/media
pictures/

# Large source assets not needed in the repo (optimized versions are committed)
assets/Hero.png
assets/dresden-hero.png

# 130 MB video exceeds GitHub's 100 MB file limit.
# The compressed web version lives in public/assets/video/.
assets/dresden-hero.mp4

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*.log

# Identity-Referenzfotos fuer Higgsfield (nicht deployen, nicht committen)
assets/identity/
